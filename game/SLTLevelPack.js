(function(window) {
	var SALTR = window.SALTR = window.SALTR || {};

	SALTR.LevelPack = function(token, index, levels) {
		this._token = token;
		this._index = index;
		this._levels = levels;
	};
})(window);
